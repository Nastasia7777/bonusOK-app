<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.hse.bonusokapplication">
    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BonusokApplication">
        <activity android:name=".DiscountDescription"></activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".EditingProfileActivity" />
        <activity android:name=".DiscountActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".AuthorizationActivity" />
        <activity android:name=".ConfirmationActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>